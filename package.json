{
  "name": "playcademy-take-home-assessment",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "prepare": "git config core.hooksPath .githooks",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "generate:voice-clips": "node --env-file-if-exists=.env tools/voice-clips-generator/generate-voice-clips.ts",
    "clean:voice-clips": "node --env-file-if-exists=.env tools/voice-clips-generator/clean-voice-clips.ts",
    "check": "svelte-check --tsconfig ./tsconfig.json"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.3",
    "@tsconfig/svelte": "^5.0.8",
    "fake-indexeddb": "^6.2.5",
    "firebase-tools": "^15.28.2",
    "svelte": "^5.20.0",
    "svelte-check": "^4.7.6",
    "typescript": "^5.9.3",
    "vite": "^6.2.0",
    "vitest": "^4.1.11"
  },
  "dependencies": {
    "firebase": "^12.18.0",
    "gsap": "^3.15.0"
  }
}

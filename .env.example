# Copy to .env (gitignored) and fill in. Used only by pre-build tools
# (tools/voice-clips-generator/) — never read by the app itself.
# Get a token at https://replicate.com/account/api-tokens
REPLICATE_API_TOKEN=

# Optional overrides:
# REPLICATE_TTS_MODEL=google/gemini-3.1-flash-tts
# REPLICATE_TTS_VOICE=

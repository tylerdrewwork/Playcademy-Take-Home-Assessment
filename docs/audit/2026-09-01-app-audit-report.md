# Full-App Audit Report — 2026-09-01

Generated from `docs/superpowers/plans/2026-09-01-full-app-audit-plan.md`. Covers all 7 planned audit areas: project structure & architecture, lesson content & problems, multiplayer game, Firebase config & deployment, Vitest/testing coverage, internal tools (voice-clips generator), and cross-cutting concerns.

## Priority Rubric

- **`critical`** — Actively broken, insecure, or blocking: data-integrity bugs in the shared multiplayer total, a write path that bypasses intended access control, a defect that can strand a student mid-lesson, or a deployment/config error that means the app cannot actually ship. Fix before anything else.
- **`high`** — Real, bounded-impact gap: incorrect/missing pedagogy or evaluation logic, an authoritative code path with no test coverage, a documented tech-stack decision that isn't actually implemented, a resource leak. Fix soon.
- **`low`** — Code quality, duplication, missing nice-to-have coverage, or a doc/reality mismatch with no real user-facing impact. Fix opportunistically.

## Summary Table

| # | Finding | File(s) | Priority | Subsystem |
|---|---|---|---|---|
| 1 | ~~`firebase.json` had no `hosting` block, only `apphosting` (a Cloud Run–backed product) — CLAUDE.md says "Hosting: Firebase Hosting."~~ **RESOLVED 2026-09-02**: confirmed via `firebase apphosting:backends:list`/`hosting:sites:list` that the App Hosting backend was disabled with no repo ever connected (nothing had ever deployed through it), while a classic Hosting site (`playcademy-assessment.web.app`) already existed. Deleted the unused App Hosting backend, added a `hosting` block to `firebase.json`, moved the Vite build output to `firebase/dist` (Hosting's `public` dir can't point outside the directory containing `firebase.json`), and deployed — verified live (HTTP 200) at https://playcademy-assessment.web.app. | `firebase/firebase.json`, `vite.config.js`, `CLAUDE.md:20` | ~~**critical**~~ → resolved | Firebase Config |
| 2 | "We do" step has no interactive practice — student is told to touch balloons but no click handler exists anywhere | `countingCombiningSteps.ts:29-31`, `CountingCombiningScreen.svelte:94-103`, `Balloon.svelte:37` | **high** | Lesson |
| 3 | Wrong-answer feedback is generic ("Not quite — try again") despite the app already computing *why* it's wrong | `LessonProblems.svelte:330-332`, `addition-1-Evaluation.ts:24-46` | **high** | Lesson |
| 4 | Problem difficulty is fixed/non-adaptive; `adaptations` field exists but is dead/unwired | `addition-1-Content.ts:31-40`, `progression.ts:29,59` | **high** | Lesson |
| 5 | No comprehension gate between instruction phases — advance is timer/narration-driven only | `CountingCombiningScreen.svelte:105-136` | **high** | Lesson |
| 6 | No automated check that `answer === sum(groups[].count)`; all 10 current problems are correct, but nothing would catch a future mistake | `addition-1-Content.ts`, `addition-1-Content.test.ts` | **high** | Lesson |
| 7 | Behavioral detectors (`detectDistraction`, `detectRageClicks`) likely misclassify normal child behavior (tracing objects while counting, excited double-taps) as distraction/rage-clicking | `behavioralDetectors.ts:7-61,103-211` | **high** | Lesson |
| 8 | `submitAnswer.ts` has no idempotency guard — a retried/duplicated call for an already-answered problem could double-award cents into the shared `totalMoney` | `submitAnswer.ts:92-127` | **high** | Multiplayer |
| 9 | Zero test coverage on any Cloud Function (649 lines across `joinGame`, `submitAnswer`, `coinProblem`, `rateLimit`, `playerNames`) — the documented single source of truth for multiplayer state; `firebase-functions-test` is installed but never wired up (no test script, no test files) | `firebase/functions/src/*.ts`, `firebase/functions/package.json` | **high** | Multiplayer / Testing |
| 10 | Coin-game jumps straight to multi-denomination coins (up to 100¢) with no built-in ramp; the only easier mode is admin-only, not student-progressed | `coinProblem.ts:1-2,6,14`, `MultiplayerScreen.svelte` | **high** | Multiplayer |
| 11 | Coin-game feedback on a wrong answer is generic, with no per-coin/denomination diagnostic | `MultiplayerScreen.svelte:297` | **high** | Multiplayer |
| 12 | "Total money" (sum of each player's own separately-counted coin pile) is a task-design mismatch with the lesson's "combine two groups" framing | `submitAnswer.ts:122-126` | **high** | Multiplayer |
| 13 | Zero test coverage on `gameSession.svelte.ts` (225 lines, the client-side hub for multiplayer session state feeding 4 UI components) | `src/multiplayer/gameSession.svelte.ts` | **high** | Testing |
| 14 | Firebase Performance Monitoring / Analytics-event logging is documented in CLAUDE.md as the crash-monitoring strategy but has zero implementation anywhere — production currently has no error/crash visibility at all | `src/**` (absent everywhere), `CLAUDE.md:27` | **high** | Cross-Cutting |
| 15 | Voice-clips generator's rate-limit pacing is coded as 1s instead of the documented/intended 10.1s — a real batch run will very likely hit Replicate's 6-req/min cap, throw `RateLimitError`, and abort the remaining batch, wasting completed spend | `tools/voice-clips-generator/generate-voice-clips.ts:89` | **high** | Internal Tools |
| 16 | Balloon-rendering accessibility: every balloon is `aria-hidden="true"` with no accessible count exposed, in a lesson whose entire task is counting objects | `Balloon.svelte:37` | low | Lesson |
| 17 | `design/counting-problems/counting-problems-mockup.html` is a 2.4MB tracked file with zero references from `src/` | `design/counting-problems/` | low | Structure |
| 18 | Design docs (`docs/superpowers/specs/*.md`) have drifted from implementation (steps→screens rename, undocumented evaluation subsystem, `jumpToPhase`/`areProblemsComplete`) — normal, healthy drift, not a defect | `docs/superpowers/specs/2026-08-27*.md`, `2026-08-28*.md` | low | Structure |
| 19 | `object: 'ball'` field in problem data is dead/unused — never read for rendering, prompt text says "balloons," `Balloon.svelte` always renders balloons regardless | `addition-1-Content.ts:31-40`, `LessonProblems.svelte:12-14` | low | Lesson |
| 20 | `Progression`'s `jumpToPhase`/`areProblemsComplete` and the whole `evaluation/` subsystem exist with no corresponding design spec | `progression.ts:70,149`, `src/lessons/evaluation/` | low | Structure |
| 21 | No component-level tests for any `.svelte` file (0/17) and no coverage-percentage tooling configured — both require an approved install to close (`@testing-library/svelte`, `@vitest/coverage-v8`) | `package.json`, `vite.config.js` | low | Testing |
| 22 | `firebase/functions/package.json` lists `firebase-functions-test` as a devDependency but it's never wired up (no `test` script, no test files) — installed-but-unused testing infra | `firebase/functions/package.json:27` | low | Firebase Config |
| 23 | `apphosting.yaml`'s env/secrets sections are fully commented out — confirmed genuinely inert, no live env vars in effect | `firebase/apphosting.yaml:10-25` | low | Firebase Config |
| 24 | No App Check configured anywhere — confirmed consistent with CLAUDE.md's documented "no anti-cheat needed yet" threat model, not an oversight | — | low | Firebase Config |
| 25 | One untracked GSAP tween (`fadeTo()` in the "combine" transition) isn't covered by the component's cleanup context — self-terminating (0.4s, no repeat) and not a confirmed persistent leak, but a real gap in the documented cleanup pattern | `CountingCombiningScreen.svelte:266-274,322,329` | low | Cross-Cutting |
| 26 | Three independent IndexedDB implementations confirmed structurally identical and intentionally separate (explicit code comments) — no behavioral inconsistency found | `progressStorage.ts`, `evaluationStorage.ts`, `musicSettingsStorage.ts` | low | Cross-Cutting |
| 27 | `tools/voice-clips-generator` regenerates every requested clip unconditionally each run — the manifest's stored `hash` is never read back to skip unchanged transcripts, wasting Replicate API credits on unmodified content unless `--only` is used manually | `generate-voice-clips.ts:151-156,260-265` | low | Internal Tools |
| — | Firebase `functions` predeploy step (`npm run lint && npm run build`) could not be executed in this isolated worktree — `firebase/functions/node_modules/` was never installed here, and per this project's install policy, npm install was not run to restore it. Config/lockfile reviewed manually and look consistent; **re-run `npm ci --prefix firebase/functions && npm run lint --prefix firebase/functions && npm run build --prefix firebase/functions`** in an authorized environment to actually confirm green. | `firebase/functions/package.json`, `package-lock.json` | **open verification item** (not a confirmed defect) | Firebase Config |
| — | No secrets (`REPLICATE_API_TOKEN` or otherwise) have ever been committed to git history; client Firebase config in `src/lib/firebase.js` is plaintext by design (non-secret, access governed by RTDB rules) | `.env` (gitignored), `src/lib/firebase.js` | — (verified clean) | Firebase Config |
| — | AdminTools/DebugOverlay/Simple-Multiplayer toggle confirmed scoped strictly to the current browser's own local IndexedDB state and its own multiplayer player-record — no other player's data or shared game state is exposed or alterable | `AdminTools.svelte`, `DebugOverlay.svelte`, `adminSettings.svelte.ts` | — (verified, not critical) | Cross-Cutting |
| — | No mismatches/dead code/gitignore issues found in general project structure sweep (config files, `App.svelte` view switch, module boundaries) — confirmed clean | — | — (no finding) | Structure |
| — | `MAX_PLAYERS=20` cap and RTDB rules (`database.rules.json`) verified correctly atomic/safe on every path checked, including `rateLimits/{uid}` | `joinGame.ts:70-90`, `database.rules.json` | — (verified safe) | Multiplayer |
| — | `problemSetVersion`/`contentVersion` bump discipline has been followed correctly in every historical commit | `addition-1-Content.ts` git history | — (verified correct) | Lesson |
| — | `npm run test` and `npm run check` both currently pass clean (133/133 tests, 0 type errors) | — | — (green baseline) | Testing |
| — | `firstPlayer`/warmup mechanism fires exactly once per empty-room transition and doesn't block the player's own UI | `joinGame.ts:69-72,103`, `gameSession.svelte.ts:139-148` | — (verified correct) | Multiplayer |
| — | No client-writable RTDB path lets a player set `totalMoney` or another player's data directly | `database.rules.json` | — (verified secure) | Multiplayer |
| — | `REPLICATE_API_TOKEN` never appears in logs, `--dry-run`/`--print-schema` output, or the manifest — no leak path found | `generate-voice-clips.ts` | — (verified clean) | Internal Tools |
| — | ffmpeg/ffprobe version/filter availability check fails loudly up front rather than silently producing bad audio | `clean-voice-clips.ts:308-330` | — (verified correct) | Internal Tools |
| — | GSAP cleanup confirmed correct in 5 of 6 GSAP-using files (`Balloon.svelte`, `LessonComplete.svelte`, `LessonProblems.svelte`, `CoinJarDeposit.svelte`, `OtherPlayerColumn.svelte`) | — | — (verified correct) | Cross-Cutting |

---

## 1. Project Structure & Architecture

**Overall:** Clean. No dead code, no gitignore/build-artifact issues, module boundaries (`src/lessons/` vs `src/multiplayer/`) are fully decoupled with zero cross-imports.

- **`dist/` and `firebase/functions/lib/`** — both correctly gitignored, no build output ever committed, no git history of either. **`low`** (confirmed clean, no action needed).
- **`App.svelte`'s hand-rolled `view` switch (no router)** — traced every writer of `view`; it only ever takes `'lesson'`/`'multiplayer'`, no orphaned state. A page reload mid-multiplayer-game returns the student to the lesson-complete screen (multiplayer state isn't restored on reload) rather than resuming the game — a UX quirk, not a dead end, since multiplayer is one click away again. **`low`**.
- **Design docs vs. implementation drift** — `docs/superpowers/specs/2026-08-27-lesson-state-tracking-design.md` and `2026-08-28-typescript-lesson-classes-design.md` predate: (a) the steps→screens rename (`Progress.instruction.currentScreenIndex` vs. the spec's `currentStepIndex`), (b) the `studentErrorTag`→`studentEvaluationTag` rename plus the entire `src/lessons/evaluation/` subsystem, which has no spec at all, and (c) `Progression.jumpToPhase`/`areProblemsComplete`. This is healthy drift (the code evolved past two dated design docs, not an unintentional divergence). **`low`**.
- **Dead code sweep** — `adminSettings.svelte.ts`, `AdminTools.svelte`, `DebugOverlay.svelte`, `svelte.d.ts` all confirmed live/used; no unused exports found. **`low`** (no issue).
- **`design/counting-problems/` mockups** — confirmed zero references from `src/` (genuinely inert design artifacts). One repo-hygiene note: `counting-problems-mockup.html` is 2.4MB and tracked in git — no functional impact (outside the Vite build graph) but permanent repo bloat. **`low`**.

## 2. Lesson & Problems

**Overall:** The problem *data* is currently correct (all 10 answers verified against their group sums) and version-bump discipline has been followed correctly in every historical commit, but the pedagogy review surfaced several real scaffolding/feedback gaps, and there's a missing validation guardrail.

- **"We do" isn't actually guided practice (`high`)** — `countingCombiningSteps.ts:29-31` narrates "Now, let's do it together! Touch the balloons as we count them," but `CountingCombiningScreen.svelte:94-103` routes the "we do" steps through the identical auto-play sequence as "I do" (just bigger numbers, 4+5 vs 2+3). `Balloon.svelte:37` is `aria-hidden="true"` with zero click handlers anywhere in the lesson. The scaffold collapses to I-do/I-do-again/you-do-at-full-stakes — the student's own counting is never exercised until the graded problems, with no checkpoint to catch a misconception earlier.
- **Generic wrong-answer feedback despite rich diagnostics (`high`)** — `LessonProblems.svelte:330-332` always shows "Not quite — try again." Meanwhile `addition-1-Evaluation.ts:24-46` already classifies *why* (partial-counting, off-by-one, far-off, invalid-input) but this signal only ever reaches the evaluation recorder for later analysis, never the student in the moment.
- **Fixed, non-adaptive difficulty (`high`)** — sums across the 10 problems are 5,5,6,7,7,8,9,7,8,7 (not ramping), and `Progress.problems.adaptations: unknown[]` (`progression.ts:29`) is seeded to `[]` and never read/written anywhere else — dead scaffolding for a feature that was apparently planned but never built.
- **No comprehension gate between instruction phases (`high`)** — intro→I-do→we-do→problems advances purely on narration-end/timer (`CountingCombiningScreen.svelte:105-136`), with no check the student is tracking and no replay affordance if lost.
- **No automated `answer === sum(groups[].count)` check (`high`)** — manually verified all 10 problems are currently correct, but neither `addition-1-Content.test.ts` nor any build step enforces this invariant; a future edit could silently ship a wrong answer key.
- **Behavioral detectors likely produce false positives for normal child behavior (`high`)** — `detectDistraction` (`behavioralDetectors.ts:7-61`) fires on ≥8s of pointer movement with no click, which is exactly what a child tracing objects to count them looks like; `detectRageClicks` (lines 103-211) can trip on excited repeat-tapping or ordinary touch-motor noise from a 7-8 year old. Thresholds read as generic defaults, not calibrated for this age group.
- **Strengths worth keeping:** problems require counting rendered objects rather than reading digits (a deliberate, well-documented v2 change matching CCSS 2.MD.C.8's "represented by groups of objects" language); retries are unlimited and never punitive; progress is legible ("Problem X of N").
- **`object: 'ball'` field is dead data, not a wording bug (`low`)** — confirmed it's never read for rendering (`LessonProblems.svelte:12-14`); balloons always render regardless of this field's value.
- **Accessibility (`low`)** — every balloon is `aria-hidden="true"` with no accessible count exposed anywhere; flagged low only because CLAUDE.md scopes this app to one known student.
- **Baseline verified green:** `npm run test -- src/lessons` → 117/117 passing at time of check (133/133 across the full suite, see §5); `npm run check` → 0 errors, 0 warnings.

## 3. Multiplayer

**Overall:** Concurrency-safety and access-control fundamentals are solid (RTDB transactions correctly serialize concurrent joins/answers, rules verified to block every bypass path checked), but there's a real correctness gap around partial failure/idempotency, zero test coverage on the authoritative backend, and pedagogy gaps in the coin game's difficulty ramp and feedback.

- **No idempotency guard on `submitAnswer` (`high`)** — `submitAnswer.ts:92-127` runs the per-player correctness transaction and the `totalMoney` increment transaction as two independent, sequentially-awaited operations with no all-or-nothing guarantee. If the function dies between the two, a player's board shows an award never reflected in the shared total. A retried/duplicated call for an already-answered problem has no guard against double-awarding cents into `totalMoney`.
- **Zero test coverage on Cloud Functions (`high`)** — confirmed no test files anywhere under `firebase/functions/` (also independently confirmed by Task 5, see §5). A minimal suite would need: seat-reuse/idempotent-rejoin/`MAX_PLAYERS`-abort/`firstPlayer` correctness for `joinGame`; correct/incorrect branching, `totalMoney` increment-on-correct-only, `warmup`/`regenerate` behavior for `submitAnswer`; feasibility/coin-count bounds for `coinProblem`; window-reset/abort behavior for `rateLimit`.
- **Coin-game difficulty has no built-in ramp (`high`)** — jumps straight to multi-denomination coins summing up to 100¢; the only easier mode (`SIMPLE_MAX_SUM=10`) is gated behind an admin-only toggle, not something the student progresses through themselves.
- **Coin-game wrong-answer feedback is generic (`high`)** — `MultiplayerScreen.svelte:297` shows one message regardless of whether the miscount was a misread coin, bad addition, or denomination confusion.
- **Task-design mismatch: "total money" vs. the lesson's "combine two groups" framing (`high`)** — the shared total sums each player's own separately-counted single pile; conceptually this is "count a mixed coin collection," not "combine two given groups," which risks it reading as a scoreboard rather than reinforcing the target skill.
- **Verified safe/correct (no action needed):** `MAX_PLAYERS=20` cap is atomic (whole-collection transaction forces true serialization — no window for 21+ joins); `database.rules.json` default-deny holds on every path checked including `rateLimits/{uid}`; no client-writable path lets a player set `totalMoney` or another player's data directly; the `firstPlayer`/warmup mechanism fires exactly once per empty-room transition and doesn't block the player's own UI.
- **Positive design choice to preserve:** unlimited retries on the same problem — no punitive restart.

## 4. Firebase Config & Deployment

**Overall:** The single biggest finding of the audit was here, and it's now resolved. Secrets handling is clean, and the remaining items are low-priority/informational.

- **Hosting vs. App Hosting mismatch — RESOLVED 2026-09-02** — `firebase/firebase.json` had no `"hosting"` block at all; only `"functions"`, `"apphosting"` (`backendId: "playcademy-assessment-app"`, `rootDir: ".."` — a Cloud Run–backed product), and `"database"`. CLAUDE.md stated plainly: "**Hosting:** Firebase Hosting." Git history ruled out "settled decision, doc just stale": the CLAUDE.md line was written in commit `903f164` (2026-08-27) *before* any Firebase config existed in the repo — a forward-looking plan, not a description of an already-built system. `firebase.json`/`apphosting.yaml` were both added the next day (`3f21ace`), and no commit ever added a `hosting` block or explained a deliberate switch. **Live-project inspection confirmed the resolution**: `firebase apphosting:backends:list` showed the App Hosting backend disabled with no repository ever connected (nothing had ever deployed through it — it was inert scaffolding, likely from `firebase init` defaulting to App Hosting rather than a deliberate choice), while `firebase hosting:sites:list` showed a classic Hosting site (`playcademy-assessment`, `https://playcademy-assessment.web.app`) already existed and was already linked to the registered web app — the actual intended target, matching CLAUDE.md. **Action taken:** deleted the App Hosting backend (`firebase apphosting:backends:delete`) and removed `apphosting.yaml`/its `firebase.json` block; added a `hosting` block (SPA rewrite to `index.html`); moved the Vite build output from root `dist/` to `firebase/dist` (Hosting's `public` dir can't point outside the directory containing `firebase.json` — `../dist` was rejected as "outside of project directory"); built and deployed via `firebase deploy --only hosting`; verified live with `curl` (HTTP 200, correct app shell) at https://playcademy-assessment.web.app.
- **No secrets ever committed (verified clean)** — `git log --all -p -S"REPLICATE_API_TOKEN"` and `-S"r8_"` (the live token's prefix) turn up nothing but the audit plan doc referencing the variable *name* and coincidental binary bytes in `.wav` files. A brief `.env.example` (added then removed) always had an empty token value. `firebase/functions/lib/` (compiled output) has never been committed. Root `.gitignore` and `firebase/functions/.gitignore` both correctly cover `.env`, `dist/`, and `lib/`.
- **`apphosting.yaml`'s env/secrets sections are inert (`low`)** — the entire `env:` block is commented out; only `runConfig.minInstances: 0` is live. No silent env vars in effect.
- **No App Check configured (`low`)** — confirmed consistent with CLAUDE.md's documented threat model (no anti-cheat needed yet), not an oversight.
- **Predeploy step could not be verified in this worktree (open item, not a confirmed defect)** — `firebase/functions/node_modules/` was never installed in this isolated audit worktree, and per this project's install policy, `npm install` was not run to restore it just to check. Config/lockfile were reviewed manually and look internally consistent (TypeScript resolves to a real, hash-verified `6.0.3`; ESLint/tsconfig setup is standard). **Recommend re-running `npm ci --prefix firebase/functions && npm run lint --prefix firebase/functions && npm run build --prefix firebase/functions`** in an environment where installing the already-locked devDependencies is authorized, to actually confirm the predeploy step is green.
- **`firebase-functions-test` installed but unused (`low`)** — listed as a devDependency in `firebase/functions/package.json` with no corresponding `test` script and no test files anywhere under `firebase/functions/` — testing infrastructure was added but never wired up. Directly relevant to the Cloud-Function test-coverage gap in §3/§5.
- **Project-ID consistency (informational)** — `.firebaserc`, `src/lib/firebase.js`'s client config, and `apphosting.yaml`'s `backendId` naming are all mutually consistent (`playcademy-assessment`/`playcademy-assessment-app`), no drift found. Client Firebase config being plaintext-committed is expected/non-secret by design.

## 5. Vitest / Testing Coverage

**Overall:** The baseline is green and the existing 13 test files are solid, but coverage has a hard boundary: nothing outside `src/lessons/` and 3 files in `src/multiplayer/` (`.ts` only) is tested at all — no Cloud Functions, no client multiplayer sync hub, no Svelte components anywhere.

- **Baseline verified green** — `npm run test`: **133/133 tests passing** across 13 test files; `npm run check`: **0 errors, 0 warnings** across 199 files. No `critical` finding here — the anticipated red-baseline scenario did not occur.
- **Zero Cloud Function test coverage (`high`)** — 649 lines across `coinProblem.ts`, `gameConfig.ts`, `index.ts`, `joinGame.ts`, `playerNames.ts`, `rateLimit.ts`, `submitAnswer.ts`, none covered by any `.test.ts` file. Confirms and cross-references §3/§4's findings on the same gap.
- **Zero coverage on `gameSession.svelte.ts` (`high`)** — 225 lines, the client-side hub for multiplayer session state (player list, coin totals), imported by 4 UI components (`OtherPlayerColumn.svelte`, `MultiplayerScreen.svelte`, `CoinScatter.svelte`, `CoinJarDeposit.svelte`), with no test file targeting it.
- **Zero component-level (`.svelte`) tests anywhere (`low`)** — 17 `.svelte` files, 0 tested; no `@testing-library/svelte` in devDependencies. `AdminTools.svelte` (reset/toggle/phase-jump state machine) and `CoinJarDeposit.svelte` (GSAP-driven timeline with a mid-timeline promise resolution) are the two components flagged as hardest to regress-check manually.
- **No coverage-percentage tooling configured (`low`)** — no `coverage` block in `vite.config.js`, no `@vitest/coverage-v8`/`-istanbul` dependency. Closing this requires an approved install; not installed here per policy.
- **`tools/voice-clips-generator/*.ts` untested (`low`)** — build-time-only tooling, never shipped in the app, so a broken generator fails a local step rather than causing a runtime regression.
- **Full manual coverage map:** `src/lessons/` (incl. `content/`, `evaluation/`) — 9 of 19 `.ts` files tested; `src/multiplayer/` — 3 of 6 `.ts` files tested; 17 `.svelte` files — 0 tested; 7 Cloud Function files — 0 tested; 2 voice-clips-generator files — 0 tested.

## 6. Internal Tools (Voice-Clips Generator)

**Overall:** No secret-leak risk, and the ffmpeg dependency fails loudly rather than silently — but a real, live rate-limiting bug was found that would very likely break large batch runs.

- **Rate-limit pacing bug (`high`)** — `generate-voice-clips.ts:89` sets `const REQUEST_PAUSE_SECONDS = 1`, but the doc comment directly above it and the README both describe the intended value as **10.1 seconds**, chosen to stay under Replicate's 6-requests/minute cap with slack for race conditions. At the current value (~10x too fast), a real run of more than a couple of clips will very likely trigger HTTP 429s, which the code treats as fatal (`RateLimitError` aborts the entire remaining batch — everything not yet attempted is marked failed). This wastes completed-so-far Replicate spend and requires manual `--only=<label>` retries for the rest. This is a live code bug, not just a stale comment — the comment/README describe the correct intended behavior; the code just doesn't implement it.
- **`manifest.json` vs. on-disk files — confirmed match, no stale data (`low`)** — this worktree's manifest (60 number entries + 9 lesson entries) matches on-disk `.wav` files exactly. One legitimate gap: `introSteps.ts` defines a 10th step (`'begin'`) with no corresponding `begin.wav`/manifest entry in this worktree — consistent with this branch predating some in-progress voice-clip work visible on `main`, not a defect; missing-clip labels are handled gracefully with a fallback (confirmed in `transcriptAudio.ts`).
- **ffmpeg/ffprobe dependency fails loudly, not silently (`low`, verified correct)** — `clean-voice-clips.ts:308-330` checks binary presence and required filter availability (`loudnorm`, `silencedetect`, `atrim`, `afade`) up front, throwing a clear named error before touching any file if anything is missing or too old.
- **No `REPLICATE_API_TOKEN` leak path found (`low`, verified clean)** — traced every use of the token variable; it's only ever interpolated into the `Authorization` request header, never logged, never touched by `--dry-run`/`--print-schema` output, and never written to `manifest.json`.
- **No hash-based skip-if-unchanged (`low`)** — the manifest stores a `hash` per clip but `generate-voice-clips.ts` never reads it back to decide whether regeneration is needed; every invocation re-spends Replicate credits on unchanged transcripts unless `--only` is used manually. Worth a future enhancement, not urgent (build-time-only tool).

## 7. Cross-Cutting

**Overall:** GSAP cleanup is solid across the app with one minor exception; the documented Performance Monitoring/Analytics strategy is entirely unimplemented; the triplicated IndexedDB pattern is intentional and harmless; and — critically for scoping risk correctly — the unauthenticated AdminTools panel was confirmed to expose and alter only the current browser's own local state, never another player's.

- **Firebase Performance Monitoring / Analytics-event logging is entirely unimplemented (`high`)** — `grep` for `getPerformance|getAnalytics|logEvent|firebase/performance|firebase/analytics` across `src/` returns zero matches. No `Sentry`, no `Crashlytics`, no `window.onerror`/`unhandledrejection` handler either. CLAUDE.md documents this as the chosen crash-monitoring strategy (since Crashlytics doesn't support Web) — production currently has **no** error/crash visibility at all, not degraded, but entirely absent.
- **One untracked GSAP tween, self-terminating (`low`)** — of 6 GSAP-using files, 5 are fully clean (`Balloon.svelte`, `LessonComplete.svelte`, `LessonProblems.svelte`, `CoinJarDeposit.svelte`, `OtherPlayerColumn.svelte` — all correctly use `gsap.context()`/explicit `killTweensOf` and revert/kill on `onDestroy`). `CountingCombiningScreen.svelte`'s main timeline is correctly scoped, but its `fadeTo()` helper (lines 266-274, called from the "combine" transition) creates a bare `gsap.to()` outside that context and isn't explicitly killed on unmount. In practice it's a single 0.4s non-repeating fade that self-completes and targets an element that becomes DOM-detached on unmount — a real gap in the documented pattern, but not a confirmed persistent leak.
- **Three IndexedDB implementations confirmed intentional, no inconsistency (`low`)** — `progressStorage.ts`, `evaluationStorage.ts`, `musicSettingsStorage.ts` are structurally identical (same open/upgrade/transaction pattern, same error handling) and explicitly documented as deliberately separate databases so each store's versioning/reset semantics doesn't constrain the others. No behavioral inconsistency found.
- **AdminTools/DebugOverlay/Simple-Multiplayer toggle exposure — confirmed local-only, not critical (`low`)** — full read of `AdminTools.svelte` plus every store/function it touches. The evaluation-log panel and phase-jump/reset actions read/write only the current browser's own local IndexedDB (`addition1EvaluationRecorder`, `addition1LessonProgress`) — no network I/O, no cross-user query. The "Simple Multiplayer" toggle is a session-scoped, unpersisted local boolean that only caps the difficulty of the coin problem written to *that same caller's own* player record via a uid-scoped Cloud Function transaction — it has zero effect on any other player's problems, scores, or the shared `totalMoney`. **No other player's data or shared game state is exposed or alterable through this unauthenticated panel**, consistent with CLAUDE.md's already-accepted "no teacher/admin authorization exists yet" position.
- **Minor UX note (`low`)** — the reset confirmation dialog only mentions "saved lesson progress" but also wipes the evaluation record in the same action; not a security issue, just an incomplete confirmation message.

---

## Long-Term / Not Deeply Investigated

(Carried from the plan's seed list — noted only, not researched further in this pass.)

- Add `@testing-library/svelte` and component-level tests (requires install approval)
- Add `@vitest/coverage-v8` and wire a coverage script (requires install approval)
- Consolidate the three IndexedDB implementations into one shared helper (low priority given they're confirmed harmless — optional cleanup, not a fix)
- Add CI to run `npm run test` + `npm run check` automatically, and ideally the Cloud Functions' `lint`/`build` too
- Consider Firebase App Check if/when the "no anti-cheat needed" threat model ever changes
- Consider whether behavioral-data logging (rage-click/distraction detection, evaluation findings) for a child user has any privacy/compliance dimension (e.g. COPPA) worth a dedicated legal/policy review
- Reconcile `docs/superpowers/specs/` design docs with implementation drift as the lesson content system evolves
- Add a hash-based skip-if-unchanged optimization to the voice-clips generator
